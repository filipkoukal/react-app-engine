import { Box } from '@mui/material';

export const Footer = () => {
    return (
        <Box
            sx={{
                flexBasis: 60,
                width: 1.0,
                padding: '10px',
                textAlign: 'center',
            }}
        >
            <div>Patička nejvíc swag.</div>
        </Box>
    );
};
